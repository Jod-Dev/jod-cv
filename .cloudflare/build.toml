[build]
command = "npm run build:cloudflare"
publish = "."

[build.environment]
NODE_VERSION = "18"
NPM_VERSION = "10"
NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
