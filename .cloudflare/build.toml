[build]
command = "npm install --legacy-peer-deps && npm run build:local"
publish = "out"

[build.environment]
NODE_VERSION = "18"
NPM_VERSION = "10"
NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
