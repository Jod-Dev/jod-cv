[build]
command = "npm run build:cloudflare"
publish = "."

[build.environment]
NODE_VERSION = "18"
NPM_VERSION = "10"

[compatibility_flags]
nodejs_compat = true

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

[site]
bucket = "."
